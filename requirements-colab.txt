# Keep Colab's CUDA-enabled torch build; pin the Hugging Face interfaces used here.
transformers==4.56.2
peft==0.17.1
accelerate==1.10.1
safetensors>=0.4.3

# Experiment, parquet, metric, configuration, and test dependencies.
numpy>=1.26,<3
pandas>=2.1,<3
pyarrow>=14
scikit-learn>=1.4,<2
PyYAML>=6.0
pytest>=8,<9
